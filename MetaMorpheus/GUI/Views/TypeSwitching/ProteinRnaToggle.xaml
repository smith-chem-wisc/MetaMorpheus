<UserControl x:Class="MetaMorpheusGUI.ProteinRnaToggle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MetaMorpheusGUI"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="180"
             Height="60" Width="180">

    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="TextBlockStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource TextColor1}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="FontSize" Value="8"/>
                <Setter Property="TextWrapping" Value="WrapWithOverflow" />
                <Setter Property="Margin" Value="0,0,0,0"/>
            </Style>
            <Style x:Key="StackPanelStyle" TargetType="StackPanel">
                <Setter Property="HorizontalAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Top"/>
                <Setter Property="Height" Value="60"/>
                <Setter Property="Margin" Value="0,0,0,0"/>
            </Style>

            <!-- Checkbox that appears as a slider -->
            <Style TargetType="{x:Type CheckBox}"
                   x:Key="SwitchCheckBoxStyle">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type CheckBox}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid Grid.Column="0"
                                      Width="40"
                                      Height="18">
                                    <!--Hitbox-->
                                    <Rectangle Fill="Transparent" />

                                    <!--Toggle-Switch Outline-->
                                    <Border CornerRadius="10"
                                            BorderBrush="Black"
                                            Background="{StaticResource LightAccentColor}"
                                            BorderThickness="1"
                                            x:Name="ToggleBorder">
                                        <!--Dot in Toggle-Switch-->
                                        <Ellipse Fill="Black"
                                                 Height="10"
                                                 Width="10"
                                                 HorizontalAlignment="Left"
                                                 Margin="4,0"
                                                 VerticalAlignment="Center"
                                                 x:Name="ToggleEllipse" />
                                    </Border>
                                </Grid>
                                <ContentPresenter Grid.Column="1"
                                                  Margin="4,0,0,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left" />
                            </Grid>

                            <ControlTemplate.Triggers>
                                <!--Change colors and move dot to the right-->
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ToggleEllipse"
                                            Property="HorizontalAlignment"
                                            Value="Right" />
                                    <Setter TargetName="ToggleBorder"
                                            Property="BorderBrush"
                                            Value="{StaticResource LightRnaAccentColor}" />
                                    <Setter TargetName="ToggleBorder"
                                            Property="Background"
                                            Value="{StaticResource LightRnaAccentColor}" />
                                    <Setter TargetName="ToggleEllipse"
                                            Property="Fill"
                                            Value="White" />

                                </Trigger>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="ToggleBorder"
                                            Property="BorderThickness"
                                            Value="1.5" />
                                    <Setter TargetName="ToggleBorder"
                                            Property="BorderBrush"
                                            Value="Black" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}"></Setter>
            </Style>
        </ResourceDictionary>
     </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch" Background="Transparent" Margin="0,0,0,0"
          Visibility="{Binding Source={x:Static local:UpdateGUISettings.Globals}, 
                        Path=ExposeRnaToUser, 
                        Converter={local:BooleanToVisibilityConverter} ,FallbackValue=Hidden}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Style="{StaticResource StackPanelStyle}" >
            <Image  Source="/Icons/protein_5.bmp" Height="40" />
            <TextBlock Text="PROTEOMICS" Style="{StaticResource TextBlockStyle}"/>
        </StackPanel>

        <CheckBox Grid.Column="1"  Name="ToggleCheckBox" VerticalAlignment="Top" HorizontalAlignment="Center"
                  IsChecked="{Binding Source={x:Static local:UpdateGUISettings.Globals}, Path=IsRnaMode, Mode=TwoWay}"
                  Style="{StaticResource SwitchCheckBoxStyle}" Margin="-10,14,-14,0" 
                  />

        <StackPanel Grid.Column="2" Style="{StaticResource StackPanelStyle}" >
            <Image   Source="/Icons/rna_200.bmp" Height="40" />
            <TextBlock Text="TRANSCRIPTOMICS" Style="{StaticResource TextBlockStyle}"/>
        </StackPanel> 
    </Grid>
</UserControl>
