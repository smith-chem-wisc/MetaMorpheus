<UserControl x:Class="MetaMorpheusGUI.ProteinRnaImageToggle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MetaMorpheusGUI"
             xmlns:guiFunctions="clr-namespace:GuiFunctions;assembly=GuiFunctions"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="100">

    <UserControl.Resources>
        <Style x:Key="BaseBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SideBarMainColor}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>
        <!-- Protein Border -->
        <Style x:Key="ProteinBorderStyle" TargetType="Border" BasedOn="{StaticResource BaseBorderStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Static guiFunctions:GuiGlobalParamsViewModel.Instance}, Path=IsRnaMode}" Value="False">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}" />
                    <Setter Property="Background" Value="{DynamicResource LightAccentColor}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Source={x:Static guiFunctions:GuiGlobalParamsViewModel.Instance}, Path=IsRnaMode}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource RnaSideBarMainColor}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <!-- RNA Border -->
        <Style x:Key="RnaBorderStyle" TargetType="Border" BasedOn="{StaticResource BaseBorderStyle}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Static guiFunctions:GuiGlobalParamsViewModel.Instance}, Path=IsRnaMode}" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource RnaAccentColor}" />
                    <Setter Property="Background" Value="{DynamicResource RnaLightAccentColor}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Source={x:Static guiFunctions:GuiGlobalParamsViewModel.Instance}, Path=IsRnaMode}" Value="False">
                    <Setter Property="Background" Value="{DynamicResource HighlightBackgroundColor}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="StackPanelStyle" TargetType="StackPanel">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
        </Style>
        <Style x:Key="TextBlockStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextColor2}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="TextWrapping" Value="NoWrap" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,1,0,0"/>
        </Style>
    </UserControl.Resources>

    <Grid HorizontalAlignment="Stretch" Background="Transparent" >

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Protein Button -->
        <DockPanel Grid.Column="0" LastChildFill="False">
            <Border  Style="{StaticResource ProteinBorderStyle}" DockPanel.Dock="Top">
                <StackPanel Style="{StaticResource StackPanelStyle}" MouseLeftButtonUp="Protein_Click">
                    <Image Source="{StaticResource ProteinIcon}" Margin="0 2 0 0"/>
                </StackPanel>
            </Border>
            <TextBlock Style="{StaticResource TextBlockStyle}" Text="Protein" DockPanel.Dock="Top" />
        </DockPanel>

        <!-- RNA Button -->
        <DockPanel Grid.Column="1" LastChildFill="False">
            <Border  Style="{StaticResource RnaBorderStyle}" DockPanel.Dock="Top">
                <StackPanel Style="{StaticResource StackPanelStyle}" MouseLeftButtonUp="Rna_Click">
                    <Image Source="{StaticResource RnaIcon}" Margin="0 2 0 0"/>
                </StackPanel>
            </Border>
            <TextBlock Style="{StaticResource TextBlockStyle}" Text="RNA" DockPanel.Dock="Top"/>
        </DockPanel>
    </Grid>
</UserControl>
