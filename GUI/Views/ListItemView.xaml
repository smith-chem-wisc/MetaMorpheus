<UserControl x:Class="MetaMorpheusGUI.ListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
             xmlns:local="clr-namespace:MetaMorpheusGUI"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="150">
    <Border  DataContext="{x:Type local:IonForTreeDisplayModel}" BorderBrush="{StaticResource ForegroundDarkBrush}" BorderThickness="2" Background="{StaticResource BackgroundColor}">
        <StackPanel Orientation="Horizontal" Background="{Binding Background}" >
            <TextBlock Text="{Binding IonName, FallbackValue='y - ion'}" Margin="5 0 5 0"/>
            <Ellipse Width="12" Height="12" Stroke="{Binding ColorBrush}" StrokeThickness="1"
                         Fill="{Binding ColorBrush}" Margin="0 5 0 5"/>
            <ComboBox ItemsSource="{Binding PossibleColors}" Width="80" HorizontalAlignment="Right" 
                          SelectedItem="{Binding SelectedColor}" SelectionChanged="ComboBox_SelectionChanged"/>
        </StackPanel>
    </Border>
    
</UserControl>
