<Window x:Class="MetaMorpheusGUI.QuantSettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MetaMorpheusGUI"
        mc:Ignorable="d"
        Title="QuantSettingWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical">
            <CheckBox x:Name="CbQuant" Content="Quantification" IsChecked="False" />
            <TextBox x:Name="txtOuantSetOut" Width="100"/>
        </StackPanel>
      
        <DataGrid x:Name="DgQuant" ItemsSource="{Binding}" Grid.Row="1">
            <DataGrid.Resources>
                
            </DataGrid.Resources>
        </DataGrid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="2">
            <Button x:Name="BtnSaveQuant" Content="Save Quantification Setting" Click="BtnSaveQuant_Click"/>
            <Button x:Name="BtnCancelQuant" Content="Cancel" Click="BtnCancelQuant_Click"/>
        </StackPanel>
    </Grid>
</Window>
